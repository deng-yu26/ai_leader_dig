# 灵山胜境 AI 数字人导游 — 开发任务清单

> 更新：2026-07-06
> 分支：ai_leader_dig `feature/zdy` / LiveTalking `feature/deep-pipeline-flush`

---

## 已完成 ✅

| 编号 | 任务 | 说明 |
|------|------|------|
| ① | 打通 LiveTalking API 链路 | `LiveTalkingService` → `/human` → 3D 口播 |
| ② | H5 前端 WebRTC 集成 | `Live2DViewer` + `rtc.js`，支持降级离线提示 |
| ③ | 实时打断 | 6 级管线冲洗 + 前端按钮 + `is_speaking` 两阶段等待 |
| ④ | 语音输入（按住说话） | `enterVoiceMode` + 静音检测 + 不发送空音频 |
| ⑤ | 管理后台数字人配置 | 扫描 `data/avatars/` + 下拉选择 + CRUD |
| ⑥ | H5 数字人切换 | Store → `Live2DViewer` → WebRTC 重连 → 切换成功 |
| ⑦ | 路线地图导航 | Leaflet+OSM 交互地图 + Canvas 预览 + 三入口连通 |
| ⑧ | 多轮对话历史 | `session_id` + `route_data` + 会话折叠 + 路线跳转 |
| ⑨ | 场景动作状态机 | welcome(仅一次) + think(提问时) + normal(默认) |
| ⑩ | RAG 知识库 | ChromaDB + 关键词检索，基础可用 |

---

## 待做 🔴

| 编号 | 任务 | 说明 | 优先级 |
|------|------|------|:--:|
| A | **通话模式** | A 模式全双工：始终开麦 + VAD 检测 → 自动打断 → ASR → 提问 | 高 |
| B | **语音输入识别修复** | 当前识别准确率不够，需调 ASR 参数或换模型 | 高 |
| C | **对话历史可见性** | 目前有时加载 500 / 网络错误，需排查数据库迁移和 API 稳定性 | 高 |
| D | **上下文管理** | LLM 多轮对话上下文记忆，目前每轮独立无关联 | 中 |
| E | **数字人形象扩充** | 目前仅 2 个可用形象，需录制更多视频生成新 avatar | 中 |
| F | **动作帧制作** | welcome / think 用占位帧，需录真正动作视频替换 | 中 |
| G | **路线提取准确性** | 关键词匹配 → LLM 结构化输出，提升地点识别精度 | 低 |
| H | **部署打包** | Docker Compose + Nginx 反向代理 | 低 |

---

## 阶段总览

| 阶段 | 内容 | 状态 |
|------|------|:--:|
| 打通链路 | 后端 ↔ LiveTalking ↔ H5 | ✅ |
| 前端改造 | WebRTC 视频 + 交互 | ✅ |
| RAG 准确率 | 知识库检索 + LLM 回答 | ⚠️ 基础可用 |
| 多模态 | 图片识别 | ⚠️ 后端已有 |
| 管理后台 | 数字人配置 + 数据大屏 | ⚠️ 基础可用 |
| 通话 + 打断 | 实时打断 + 语音输入 + A 模式 | 🔴 打断/语音完成，A 模式待做 |
| 路线导航 | AI 路线 + 地图展示 | ✅ |
| 动作系统 | 场景动作状态机 | ✅ 框架完成，待录素材 |
| 文档 + 演示 | 设计文档 + PPT + 视频 | 🔴 |
