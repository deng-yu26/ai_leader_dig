# 灵山胜境 AI 数字人导游系统 — 核心功能文档

> 最后更新：2026-07-06  
> 项目路径  
> - `ai_leader_dig/` — 导游大脑（Flask + Vue3），分支 `feature/zdy`  
> - `LiveTalking/` — 3D 渲染引擎（aiohttp + PyTorch），分支 `feature/deep-pipeline-flush`

---

## 一、项目架构

```
┌─────────────────────────────────────────────────────────┐
│                   H5 游客端 (Vue3 + Vant4)               │
│  Home.vue · DigitalHuman.vue · History.vue · RouteMap   │
│  端口 :3000                                              │
└──────────┬───────────────────────────────┬──────────────┘
           │ WebSocket (chat_ws.py)        │ HTTP API
           ▼                               ▼
┌─────────────────────────────────────────────────────────┐
│               ai_leader_dig 后端 (Flask)                 │
│  RAG 知识库 → LLM 智谱 GLM → TTS → LiveTalkingService   │
│  端口 :5001                                              │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP API
                           ▼
┌─────────────────────────────────────────────────────────┐
│             LiveTalking 3D 引擎 (aiohttp)                │
│  TTS → 音频特征提取 → AI 口型推理 → WebRTC 推流          │
│  端口 :8010                                              │
└─────────────────────────────────────────────────────────┘
```

### 启动命令

```bash
cd LiveTalking              && python app.py    # 1. 3D 引擎
cd ai_leader_dig/backend    && python app.py    # 2. 后端
cd ai_leader_dig/h5         && npm run dev      # 3. H5
cd ai_leader_dig/admin      && npm run dev      # 4. 管理后台
```

### 1.1 数据流

```
Home.vue                       Live2DViewer.vue                LiveTalking
  │                                  │                             │
  │  ──── WebSocket 提问 ────→ 后端  │                             │
  │                                  │                             │
  │  ←── text_chunk ────            │  ←── WebRTC 视频流 ────    │
  │  ←── audio (lt_synced) ──       │                             │
  │                                  │                             │
  │        ┌─────────────────────────┤                             │
  │        │ <video srcObject=stream>                              │
  │        │ 3D 导游口播画面                                        │
```

### 1.2 双通道表情动作驱动

- **底层 Audio2Face**：TTS 语音 → 自动生成同步唇形、眨眼、头部微动
- **上层 LLM 语义情感**：大模型输出情绪标签 → 切换面部表情 + 肢体手势
- 全本地离线推理，无第三方付费 API，无 Token 消耗

### 1.3 端口

| 服务 | 端口 |
|------|------|
| LiveTalking 3D 引擎 | 8010 |
| ai_leader_dig 后端 | 5001 |
| H5 游客端 | 3000 (Vite) |
| 管理后台 | 5173 (Vite) |

### 1.4 显卡要求

- 有 NVIDIA 显卡 → MuseTalk 模式（流畅，推荐）
- 无显卡 → Wav2Lip 轻量模式（可用，稍卡）

---

## 二、实时打断系统

### 2.1 深度管线冲洗

打断时清空 TTS → ASR → 特征队列 → 渲染队列 → WebRTC 发送队列 全部 6 级：

```
flush_talk()
  ├─ TTS._cancel_event.set()    ← 取消正在进行的 TTS 流
  ├─ TTS.msgqueue.clear()       ← 清消息队列
  ├─ ASR.queue.clear()          ← 清输入队列
  ├─ ASR.feat_queue.clear()     ← 清特征队列
  ├─ ASR.output_queue.clear()   ← 清输出队列
  ├─ ASR.frames.reset()         ← 重置缓冲区 + 静音预热
  ├─ res_frame_queue.clear()    ← 清渲染队列
  ├─ output.flush()             ← 清 WebRTC 发送队列
  └─ speaking = False           ← 重置状态
```

**关键文件**：`LiveTalking/avatars/base_avatar.py`、`tts/base_tts.py`、`avatars/audio_features/base_asr.py`、`server/webrtc.py`、`streamout/webrtc.py`

### 2.2 前端打断按钮

- 数字人思考+朗读时**始终可见**（`v-if="isProcessing || isSpeaking"`）
- `lt_synced` 标志通过 `websocket.js` 正确传递
- 后端**两阶段等待**：先等 `is_speaking=true`（LiveTalking 开始说），再等 `is_speaking=false`（说完），才发 `done`

### 2.3 三种打断途径

| 方式 | 实现 |
|------|------|
| 点击 ⏹ 按钮 | `stopReply()` → WebSocket 发 interrupt → LiveTalking 断 |
| 打字发送 | `sendText()` 检测 `isSpeaking` → 自动打断 + 发新问题 |
| (待实现) 语音打断 | A 模式 VAD 检测 → 自动打断 |

---

## 三、语音输入（按住说话）

### 3.1 交互

```
空闲:   [📷]  [___输入问题...___]   [🎤]  [➤]
语音模式: [📷]  [🎙️ 请按住说话]       [✕]  [➤]
录音中:  [📷]  [🎙️ 正在聆听...]       [✕]  [➤]   ← 红色按压态
```

### 3.2 静音检测

| 条件 | 行为 |
|------|------|
| 时长 < 0.5s | 误触 → toast "未检测到声音，请重试" |
| 数据 < 2KB | 无声 → 不发送 |
| 正常 | WebSocket 发送 base64 音频 → 后端 ASR → 提问 |

### 3.3 关键文件

- `h5/src/views/Home.vue`：`enterVoiceMode()`、`startHoldSpeak()`、`stopHoldSpeak()`
- `h5/src/components/Live2DViewer.vue`：WebRTC 视频渲染 + 可选绿幕抠像

---

## 四、数字人场景动作（状态机）

### 4.1 目录结构

```
data/avatars/{avatar_id}/
├── actions.json          ← 该数字人的动作配置
├── actions/
│   ├── welcome/          ← 欢迎帧 + audio.wav（可选的配音）
│   └── think/            ← 思考帧（循环，无音频）
└── full_imgs/            ← normal 待机帧（默认，无需额外配置）
```

### 4.2 actions.json

```json
[
  {"scene": "welcome", "audiotype": 2, "imgpath": "actions/welcome", "audiopath": "actions/welcome/audio.wav"},
  {"scene": "think",    "audiotype": 3, "imgpath": "actions/think"}
]
```

### 4.3 状态机

```
用户进入 ──→ idle (audiotype=1, full_imgs 待机)
                │
          首次提问
                │
        ┌───────┴────────┐
        │ 欢迎 welcome   │  audiotype=2, 后台线程播放，只播一次
        │ 思考 think     │  audiotype=3, 循环播放思考动作帧
        └───────┬────────┘
                │
         LLM 生成第一句 → 发给 LiveTalking
                │
        lt.set_audiotype(0)  →  正常说话 (AI 实时口型同步)
                │
        说话结束 → idle (audiotype=1)
                │
          再次提问 → think (跳过 welcome)
```

### 4.4 打断行为

| 场景 | 行为 |
|------|------|
| thinking 中被中断 | `flush_talk()` → `custom_audiotype=0` → 新问题 → think 重启 |
| speaking 中被中断 | `flush_talk()` → `custom_audiotype=0` → 新问题 → think |

### 4.5 制作动作帧

```bash
ffmpeg -i welcome.mp4  -vf fps=25 data/avatars/1/actions/welcome/%05d.jpg
ffmpeg -i thinking.mp4 -vf fps=25 data/avatars/1/actions/think/%05d.jpg
```

### 4.6 关键文件

- `LiveTalking/avatars/base_avatar.py`：`__loadcustom()` 自动加载 per-avatar `actions.json`、`set_custom_state()` 支持退出到 normal
- `LiveTalking/avatars/audio_features/base_asr.py`：`get_audio_frame()` 处理无音频动作
- `backend/websocket/chat_ws.py`：`welcome_played` 标志 + `lt_speak_and_normal()` helper

---

## 五、地图路线导航

### 5.1 技术栈

- **Canvas** 路线预览（无依赖）
- **Leaflet + OpenStreetMap** 交互地图（免费，无需 API Key）
- **高德 URI Scheme** 跳转手机原生导航

### 5.2 交互流程

```
用户问"怎么去灵山大佛"
  ↓ LLM 纯自然语言回答（不含 JSON 格式）
  ↓ extract_route_info() 关键词 + 规则检测路线意图
  ↓ 后端发 MSG_TYPE_ROUTE 到前端
  ↓ AI 气泡下出现 [🗺️ 查看路线] 按钮
  ↓ 点击 → 直接跳转 RouteMap 页面
  ↓
RouteMap 页面:
  ├─ 📋 AI 导游建议
  ├─ 🗺️ 路线预览（CSS 节点 + 连线）
  ├─ 📍 Leaflet 交互地图（免费 OSM 瓦片，可缩放拖拽）
  ├─ [🧭 跳转地图App导航] → 高德 URI
  └─ [📍 获取我的位置] → 浏览器 Geolocation API
```

### 5.3 三个入口

| 入口 | 数据来源 |
|------|---------|
| Home 对话 | LLM → `extract_route_info()` → route 消息 |
| RoutePlan 预设路线 | 本地 JSON → 解析 spots 名称 |
| History 历史会话 | `ChatLog.route_data` 字段 |

### 5.4 景区坐标

`RouteMap.vue` 的 `SPOT_COORDS` 硬编码了灵山 14 个景点经纬度。新景点加一行即可。

### 5.5 关键文件

- `backend/websocket/chat_ws.py`：prompt 不含 JSON + answer 清理 + route 消息
- `backend/services/llm_service.py`：`extract_route_info()`、`_looks_like_route()`、`_build_route_data()`
- `h5/src/views/RouteMap.vue`：Leaflet + OSM 交互地图
- `h5/src/views/RoutePlan.vue`：`openRouteOnMap()` 解析 spots
- `h5/src/views/History.vue`：`openRoute()` 读取历史 route_data

---

## 六、多轮对话历史

### 6.1 数据模型

```sql
ChatLog:
  session_id   VARCHAR(64)  -- 同一 WebSocket 连接共享
  route_data   TEXT         -- 路线 JSON（可选）
  user_id, question_text, answer_text, emotion_label, ...
```

### 6.2 前端展示

```
┌─────────────────────────────────┐
│ 💬 灵山大佛怎么去         🗺️  ▼ │  ← 会话卡片（可折叠）
│   07-06 15:30 · 3 轮对话        │
├─────────────────────────────────┤
│ 🙋 灵山大佛怎么走               │  ← 展开的消息
│ 🤖 从南门出发...                │
│ ───────────────────             │
│ 🙋 佛足坛要门票吗               │  ← 同一会话的第 2 轮
│ 🤖 佛足坛是免费的...            │
└─────────────────────────────────┘
```

一个 WebSocket 连接 = 一个 session_id = 一次会话，包含多轮 QA。

### 6.3 关键文件

- `backend/models.py`：ChatLog 新增 `session_id`、`route_data` 列
- `backend/routes/user_routes.py`：`get_chat_history()` 按 session 分组返回
- `h5/src/views/History.vue`：`toggleSession()` 折叠展开 + `openRoute()` 跳转地图

---

## 七、数字人形象管理

### 7.1 生成新形象

录一段 **3-5 分钟正脸说话视频**（口齿清晰、背景干净），两种方式生成：

**方式 A：Web 界面**
打开 `http://127.0.0.1:8010/avatar.html`，上传视频即可。

**方式 B：命令行**
```bash
cd LiveTalking
python avatars/musetalk/genavatar.py --file your_video.mp4 --avatar_id my_avatar
```

生成后 `data/avatars/my_avatar/` 下会出现：`full_imgs/`（待机帧）、`coords.pkl`（脸坐标）、`latents.pt`（AI 推理向量）、`mask/`（脸部遮罩）。

**多形象**：录制不同的人物/服装/场景视频，分别生成到不同文件夹名。

### 7.2 形象切换原理

LiveTalking `/offer` 接口支持按会话切换：

```python
# app.py
avatar_id = params.get('avatar', opt.avatar_id)  # offer 带 avatar 就切换
```

前端 WebRTC 握手时传 `"avatar": "形象名"` → LiveTalking 加载 `data/avatars/{形象名}/`。**不需要重启 LiveTalking**，断开重连即可切换。

### 7.3 管理后台配置

```
数字人管理页面：
  ├─ 名称（如"女导游"）
  ├─ Avatar 文件夹 —— 下拉选择（自动扫描 data/avatars/）
  ├─ 语速 / 语调 / 音色
  ├─ 启用状态
  └─ [确认保存]
```

### 7.2 H5 切换链路

```
管理后台 → DigitalHuman.model_path（如 "1"）
  → API /api/user/digital-humans
  → H5 Store → getCurrentAvatarId()
  → Live2DViewer.connectLiveTalking("1", ...)
  → POST /offer { avatar: "1" }
  → LiveTalking 加载 data/avatars/1/
  → WebRTC 画面切换 ✅
```

### 7.3 关键文件

- `backend/routes/admin_routes.py`：`/avatars-on-disk` 扫描 LiveTalking 目录
- `backend/config.py`：`LIVETALKING_AVATARS_DIR` 自动探测
- `h5/src/utils/rtc.js`：WebRTC offer 带 `avatar` 参数
- `h5/src/store/index.js`：`getCurrentAvatarId()` 返回 `model_path`
- `h5/src/components/Live2DViewer.vue`：`dhId` watch → 重连
- `admin/src/views/DigitalHumanManage.vue`：下拉选择 + 保存

---

## 八、绿幕抠像

`Live2DViewer.vue` 支持 Canvas 实时抠像：

```html
<Live2DViewer :chrome-key="true" key-color="#00FF00" :threshold="0.3" />
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `chromeKey` | `false` | 开关 |
| `keyColor` | `#00FF00` | 要抠的颜色 |
| `threshold` | `0.3` | 容差 0-1 |

原理：WebRTC 视频流 → 隐藏 `<video>` → Canvas 逐帧读像素 → 匹配色设 alpha=0 → 透明背景

---

## 九、常见问题

| 问题 | 解决 |
|------|------|
| 管理后台 401 | 删库重建后需重新登录：`admin` / `admin123` |
| 历史记录 500 | 旧数据库缺少 `session_id`/`route_data` 列 → 删 `lingshan.db` 重启 |
| Avatar 下拉无数据 | 确认 `LIVETALKING_AVATARS_DIR` 路径正确，LiveTalking 已启动 |
| 动作不触发 | 确认 `actions/` 目录有帧图片 + 重启 LiveTalking |
| 打断按钮消失 | 检查后端 console 是否有 `[LiveTalking] 开始说话` 日志 |

---

## 十、分支

| 项目 | 分支 | 核心改动 |
|------|------|---------|
| ai_leader_dig | `feature/zdy` | 打断、语音、路线、历史、动作状态机 |
| LiveTalking | `feature/deep-pipeline-flush` | 管线冲洗、per-avatar actions、set_custom_state 修复 |
