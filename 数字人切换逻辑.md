
用户进入页面
  → 自动触发 audiotype=2（欢迎动作）
  → 数字人播放欢迎动画 + "你好！欢迎来到灵山胜境..."
  → 音频播完自动回到待机状态（audiotype=1）

用户提问
  → TTS 生成音频 → 自动切 audiotype=0（口型说话）
  → 回答完毕 → 自动回到待机状态（audiotype=1）

不同场景切换
  → 后端根据 LLM 情绪/意图调 lt.set_audiotype(n, sessionid)
  → 比如：告别 audiotype=4，惊喜 audiotype=3

你需要准备的

只需要准备图片帧（从视频抽帧即可）：

# 用 ffmpeg 从视频抽帧
ffmpeg -i welcome.mp4 -vf fps=25 data/custom/welcome/%05d.jpg
ffmpeg -i goodbye.mp4 -vf fps=25 data/custom/goodbye/%05d.jpg

然后启动 LiveTalking 时加载配置：